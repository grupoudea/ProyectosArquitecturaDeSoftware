spring.datasource.url               = jdbc:mariadb://localhost:3306/saetel-db
spring.datasource.username          = root
spring.datasource.password          = 14
spring.datasource.driver-class-name = org.mariadb.jdbc.Driver

spring.jpa.database-platform = org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto= none
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.jdbc.time_zone = America/Bogota

server.port = 8080
server.servlet.context-path = /api



spring.session.store-type             = jdbc # Session store type
#server.servlet.session.timeout        = # Session timeout. If a duration suffix is not specified, seconds are used.
spring.session.jdbc.initialize-schema = embedded # Database schema initialization mode.
#spring.session.jdbc.schema            = classpath:org/springframework/session/jdbc/schema-@@platform@@.sql # Path to the SQL file to use to initialize the database schema.
spring.session.jdbc.table-name        = sesión_spring # Name of the database table used to store sessions.

server.error.include-stacktrace = always